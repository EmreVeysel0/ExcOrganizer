@page
@model LoginModel
@{
    ViewData["Title"] = "Вход";
    Layout = "/Views/Shared/_Layout.cshtml";
}

<div class="row justify-content-center mt-4">
    <div class="col-md-5 col-lg-4">

        <div class="card shadow-sm border-0" style="border-radius:12px; overflow:hidden;">

            <div class="card-header text-white py-3 text-center"
                 style="background:linear-gradient(135deg,#1a73e8,#0d47a1);">
                <i class="bi bi-box-arrow-in-right fs-3 d-block mb-1"></i>
                <h5 class="mb-0 fw-bold">Вход в системата</h5>
            </div>

            <div class="card-body p-4">

                <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2 small" role="alert"></div>

                <form id="account" method="post">

                    <div class="mb-3">
                        <label asp-for="Input.Username" class="form-label fw-semibold small">
                            <i class="bi bi-person me-1 text-primary"></i> Потребителско име или имейл
                        </label>
                        
                        <input asp-for="Input.Username" type="text" class="form-control"
                               autocomplete="username"
                               placeholder="ivan123" />
                        <span asp-validation-for="Input.Username" class="text-danger small"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Input.Password" class="form-label fw-semibold small">
                            <i class="bi bi-lock me-1 text-primary"></i> Парола
                        </label>
                        <input asp-for="Input.Password" class="form-control"
                               autocomplete="current-password"
                               placeholder="••••••••" />
                        <span asp-validation-for="Input.Password" class="text-danger small"></span>
                    </div>

                    <div class="mb-4 d-flex justify-content-between align-items-center">
                        <label class="d-flex align-items-center gap-2 mb-0" style="cursor:pointer;">
                            <input class="form-check-input m-0" asp-for="Input.RememberMe" />
                            <span class="small text-muted">Запомни ме</span>
                        </label>
                        <a href="/Identity/Account/ForgotPassword" class="small text-primary">
                            Забравена парола?
                        </a>
                    </div>

                    <button type="submit" class="btn w-100 py-2 fw-bold text-white"
                            style="background:linear-gradient(135deg,#1a73e8,#0d47a1); border:none; border-radius:8px;">
                        <i class="bi bi-box-arrow-in-right me-1"></i> Влез
                    </button>

                </form>

            </div>

            <div class="card-footer text-center py-3" style="background:#f8fafc;">
                <span class="text-muted small">Нямаш акаунт?</span>
                <a href="/Identity/Account/Register" class="fw-bold ms-1" style="color:#1a73e8;">
                    Регистрирай се
                </a>
            </div>

        </div>

    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}